version: '3.4'

services:
  quadro-tech-front:
    image: 
    container_name: 
    hostname: 
    restart: 
    networks:
      video_net:
#    environment:
#      - ASPNETCORE_ENVIRONMENT=docker
    healthcheck:
     test: 
     interval: 5s
     timeout: 10s
     retries: 3
    ports:
    


networks:
  video_net:
    driver: 
